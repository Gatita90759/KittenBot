
module.exports = {
  TOKEN: process.env.TOKEN,
  CLIENT_ID: process.env.CLIENT_ID,
  GUILD_ID: process.env.GUILD_ID,
  prefix: 'kb!', // Update the prefix to '!'
  rewards: {} // Add empty rewards object for level roles
};